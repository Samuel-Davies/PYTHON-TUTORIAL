# trying to write an algorithm that gets a list a size and then appends to the empty list and prompts user that it full when maxlength is reached


size  = int(input('Enter book max size length $ '))
book = list().append(input('enter the elements of the array:'))

print()
